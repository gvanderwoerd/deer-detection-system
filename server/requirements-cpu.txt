# Deer Detection System - Python Dependencies (CPU-only)
# Optimized for systems without NVIDIA GPU (~1.7GB install vs 5.4GB)
#
# Installation:
#   pip install -r requirements-cpu.txt
#
# Note: PyTorch CPU-only must be installed separately with:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

# Computer Vision and AI
opencv-python>=4.8.0
ultralytics>=8.0.0

# PyTorch (install separately - see note above)
# torch>=2.0.0+cpu
# torchvision>=0.15.0+cpu

# Web Server
flask>=3.0.0
flask-socketio>=5.3.0
python-socketio>=5.9.0
eventlet>=0.33.0

# Smart Home Integration
tinytuya>=1.13.0

# Utilities
pillow>=10.0.0
numpy>=1.24.0
requests>=2.31.0
