# Deer Detection System - Python Dependencies
#
# DEPRECATED: Use hardware-specific requirements files instead:
#   - requirements-cpu.txt (500MB, no GPU - recommended for most systems)
#   - requirements-gpu.txt (5GB, NVIDIA GPU + CUDA support)
#
# The start.sh script automatically detects your hardware and uses the correct file.
#
# For manual installation:
#   pip install -r requirements-cpu.txt   (CPU-only)
#   pip install -r requirements-gpu.txt   (GPU-accelerated)

# Computer Vision and AI
opencv-python>=4.8.0
ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0

# Web Server
flask>=3.0.0
flask-socketio>=5.3.0
python-socketio>=5.9.0
eventlet>=0.33.0

# Smart Home Integration
tinytuya>=1.13.0

# Utilities
pillow>=10.0.0
numpy>=1.24.0
requests>=2.31.0
